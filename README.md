# Simple Counter

A vanilla web project that lets users increase, decrease, and reset values.

## Use

- Make sure Python 3 is installed.
- Clone this repository.
- Run the following:
```
python -m http.server 3000
```
- Open (Localhost)[http://localhost:3000] in your browser.
